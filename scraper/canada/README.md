# Canada scraper

Uses Uniqlo Canada's English storefront API with 27 men, women, kids, and baby
categories. Prices are emitted as `CA $ 19.90` for compatibility with the
current ingestion API.

From the parent `scraper` directory, run:

```bash
python canada/main.py
```
