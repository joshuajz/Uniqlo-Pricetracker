# UK scraper

Uses Uniqlo UK's English storefront API with 23 men, women, and kids categories.
The storefront's live client identifier is `uq.gb.web-spa`; prices are emitted
in GBP, such as `£19.90`.

From the parent `scraper` directory, run:

```bash
python uk/main.py
```
